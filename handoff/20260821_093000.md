Handoff Summary — 2026-08-21 (3)

## Cosa è cambiato

### Soppressione 3 prompt NEW da build non-interattiva

Durante la compilazione manuale del kernel sono apparsi 3 prompt NEW non presenti nei fragment:

| Prompt | Dipende da | Scelta | Fragment |
|---|---|---|---|
| `FUSE_PASSTHROUGH` | `FUSE_FS=m` (aggiunto in sessione precedente) | `=y` (migliora performance) | `docker.fragment` |
| `VIRTIO_FS` | `FUSE_FS=m` | `=n` (serve solo per VM) | `docker.fragment` |
| `OVERLAY_FS_NFS_EXPORT` | `OVERLAY_FS_INDEX=y` (aggiunto in sessione precedente) | `=n` (NFS export non serve per container) | `rootless.fragment` |

### Verifica

`make config && make config_patch` → **0 prompt, 0 NEW** ✓

### Commit

```
4a9eaf5 fragment: suppress FUSE_PASSTHROUGH, VIRTIO_FS, OVERLAY_FS_NFS_EXPORT prompts
```

## Contesto sessione

### Analisi rootfs /mnt/void/ (Void Linux + glibc)

Stato della rootfs USB montata sul desktop:

- **Hostname**: era "void" → da cambiare in "ouya"
- **Servizi runit**: mancavano sshd e dhcpcd-eth0 in `/etc/runit/runsvdir/default/`
- **SSH**: sshd_config aveva `PermitRootLogin prohibit-password` → da cambiare in `yes`
- **Root password**: da impostare ("frisedda")
- **SSH host keys**: verranno generate al primo boot dallo script sshd run
- **resolv.conf**: non presente, dhcpcd lo crea al boot
- **Fstab**: UUID root ext4 corretto

Lista comandi per la configurazione è stata fornita all'utente (hostname, symlink servizi, hash password via openssl, sed su sshd_config).

### Configurazione kernel aggiornata

10 fragment attivi nel Makefile, tutti testati con 0 prompt:
1. docker.fragment (→ FUSE, FUSE_PASSTHROUGH)
2. iptables_qos.fragment
3. notuner.fragment
4. ouya.fragment
5. usbserial.fragment
6. bluetooth.fragment
7. usb_gadget.fragment
8. rootless.fragment (→ TUN, OVERLAY_FS_INDEX, OVERLAY_FS_NFS_EXPORT)
9. security.fragment

### Issue aperte

1. **wireless.fragment** — commentato nel Makefile, 100% ridondante con tegra_defconfig
2. **USB host patch** — funziona su v6.6.2 e v6.12.104, per v7.x serve revert parziale diverso
3. **Rootfs /mnt/void/** — configurazione in corso (hostname, servizi, password)

## Prossimi passi

1. Eseguire i comandi sulla rootfs (hostname, symlink, password, sshd_config)
2. Boot della OUYA con il nuovo kernel
3. Verificare SSH: `ssh root@ouya` con password "frisedda"
4. Installare firmware BCM4330 (WiFi + BT) su rootfs via SSH
5. Installare applicazioni (docker, python, goprostream) via xbps
