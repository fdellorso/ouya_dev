Handoff Summary — 2026-08-20

## What changed

- Created `docs/AUDIT_REPORT_V2.md` — supersedes AUDIT_REPORT.md; corrects two major errors (kernel_dtb path, USB PHY parameters)
- Updated `opencode.json` — removed irrelevant watcher ignore paths (.pi, .private, firmware/.pio)
- Updated `.opencode/package.json` — @opencode-ai/plugin 1.17.9 → 1.18.18
- Updated `~/.config/opencode/package.json` — @opencode-ai/plugin 1.4.1 → 1.18.18
- Updated `.opencode/skills/ouya-kernel/SKILL.md` — corrected USB PHY description (parameters exist in tegra30.dtsi, issue is values not presence)
- Updated `AGENTS.md:5` — replaced `docs/ISSUES.md` (doesn't exist) with `docs/AUDIT_REPORT_V2.md`

## What was tested

- Read-only audit of entire repository (phases 1-12)
- Cross-validated audit with independent report
- Verified DTS overlay inheritance: tegra30.dtsi USB PHY params (nvidia,xcvr-*) are inherited by tegra30-ouya.dts
- Verified CONFIG_ARCH_WANT_FLAT_DTB_INSTALL=def_bool y on ARM confirms kernel_dtb path is correct
- Checked skills.sh for relevant skills — none applicable (web/frontend focused)
- Verified opencode infrastructure: skills, commands, MCP, LSP all present

## Open issues

1. **USB PHY values** — parameters exist in DTS (via tegra30.dtsi), but values may be wrong for OUYA vs decatf fork. Compare nvidia,xcvr-setup, nvidia,hsdiscon-level values between mainline tegra30.dtsi and https://github.com/decatf/linux DTS.
2. **ouya.fragment spontaneous truncation** — documented in handoff/20260626_094800.md, happened 3 times. Root cause unknown. Check integrity before every config_patch.
3. **submodule-linux hardcoded** — Makefile:195 still has v6.12.91, submodule is at v6.12.99. Needs update to read from .gitmodules.
4. **No initramfs** — brcmfmac is module (=m), won't load at boot without initramfs. WiFi requires manual modprobe after boot.

## Next steps

1. Compare USB PHY parameter values between tegra30.dtsi and decatf fork — if different, add overrides to tegra30-ouya.dts
2. Fix Makefile:195 to read branch from .gitmodules instead of hardcoded tag
3. Investigate ouya.fragment spontaneous truncation (add checksum verification before config_patch)
4. Commit the 6 file changes made this session
