Handoff Summary — 2026-06-26 09:48

## What changed

Modified files (4 fragments, all uncommitted):
- `linux-config/fragment/security.fragment` — added LSM stack + module signing opts (22 lines)
- `linux-config/fragment/docker.fragment` — added disabled networking/cgroup opts (29 lines, skipped `CONFIG_NET_SCH_DEFAULT`/`CONFIG_DEFAULT_PFIFO_FAST` per user decision — already in `iptables_qos.fragment`)
- `linux-config/fragment/usbserial.fragment` — added all disabled USB serial drivers (50 lines)
- `linux-config/fragment/usb_gadget.fragment` — added `# CONFIG_USB_CONFIGFS_SERIAL is not set` (1 line)

Also present (pre-existing, not ours):
- `linux-config/fragment/ouya.fragment` — lost 8 lines again (USB_EHCI_TEGRA, CHIPIDEA_*, USB_ROLE_SWITCH). Same spontaneous truncation as noted in previous handoff.

## What was tested

- `make config` (tegra_defconfig) + `make config_patch` → completed, no `(NEW)` prompts
- `bash linux-config/check-config.sh linux-build/.config` → OK (only zfs missing, expected)
- Grep verification: all new options present in `linux-build/.config` with expected values
- Fragment backup saved as `/tmp/config-before-newopts`

## Open issues

1. **ouya.fragment spontaneous truncation** — happened again (3rd occurrence across sessions). Lines 17-24 (USB_EHCI_TEGRA, CHIPIDEA_*, USB_ROLE_SWITCH) disappeared from the file. Check before every `config_patch` run.
2. **Usb host instability** (pre-existing) — DTS PHY trim parameters missing in `tegra30-ouya.dts`, not a Kconfig issue.
3. **wireless.fragment** — 100% redundant with `tegra_defconfig`, kept by choice.
4. **make kernel_dtb path** — `Makefile:173` needs `nvidia/` subdir for kernel 6.12 DTB install path.

## Next steps

- Commit the 4 modified fragments (after restoring ouya.fragment if needed)
- Verify USB PHY trim params in DTS
- Fix make kernel_dtb path
