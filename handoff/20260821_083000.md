Handoff Summary — 2026-08-21 (2)

## Cosa è cambiato

### Aggiunta FUSE a docker.fragment

Aggiunto `CONFIG_FUSE_FS=m` e `# CONFIG_CUSE is not set` alla fine di `docker.fragment`. FUSE serve per fuse-overlayfs in rootless mode e per qualsiasi tool userspace che usa FUSE.

### Nuovo fragment rootless

Creato `linux-config/fragment/rootless.fragment` con i requisiti kernel per Docker/Podman rootless:

- `CONFIG_TUN=y` — `/dev/net/tun` per slirp4netns e pasta (networking rootless)
- `CONFIG_OVERLAY_FS_INDEX=y` — overlay nativo unprivileged per rootless storage
- `# CONFIG_TUN_VNET_CROSS_LE is not set` — sopprime build prompt

### Aggiornamento Makefile

Aggiunta riga per `rootless.fragment` nel target `config_patch`, dopo `usb_gadget.fragment` e prima di `security.fragment`. Ordine finale: 10 fragment attivi.

### Sostituzione README.md

Sostituito il README.md inglese con versione italiana completa che documenta:
- Tutte le funzionalità kernel attive (wireless, BT, Docker/Podman rootful/rootless, networking, USB, storage, thermal, security, audio)
- Tabella comparativa requisiti kernel per container runtime rootful vs rootless
- Patch USB host (problema, root cause, soluzione, come applicare, versioni testate)
- Firmware richiesti (WiFi e BT)
- Build workflow completo
- Struttura del repository
- Limitazioni note

### Build testata

```
make config → make config_patch → make kernel → 0 prompt ✓
```

- zImage: 10.9 MB
- fuse.ko: compilato in `kernel/fs/fuse/fuse.ko`
- check-config.sh: tutti i check Docker passati

## Cosa non è cambiato

- `linux/` submodule — stato dirty preesistente (patch USB host)
- `reference/` — non modificato
- `patches/` — già presenti

## Issue aperte

1. **wireless.fragment** — 100% ridondante con tegra_defconfig, commentato nel Makefile. Potrebbe essere eliminato.
2. **USB host patch** — funziona su v6.6.2 e v6.12.104. Per v7.x serve un revert parziale diverso di host.c.

## Prossimi passi

1. Test boot su hardware Void Linux con firmware BCM4330
2. Verificare Docker rootless su Void Linux
3. Verificare Podman rootless su Void Linux
4. Valutare eliminazione definitiva di `wireless.fragment`
