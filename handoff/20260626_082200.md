Ecco il riepilogo di handoff:
Handoff Summary — 2026-06-26
What changed
Modified files (5 fragments, all uncommitted):

- linux-config/fragment/bluetooth.fragment — 7 redundant CONFIG\_\*=y removed (were already in tegra_defconfig)
- linux-config/fragment/docker.fragment — ~205 # CONFIG\_\* is not set lines removed (were already default-"not set" in tegra_defconfig)
- linux-config/fragment/iptables*qos.fragment — 6 # CONFIG*\* is not set lines removed (ditto)
- linux-config/fragment/usb*gadget.fragment — 19 # CONFIG*\* is not set lines removed (ditto)
- linux-config/fragment/usbserial.fragment — ~50 # CONFIG\_\* is not set lines removed (ditto)
  No DTS changes, no kernel version changes.
  New files (untracked):
- docs/AUDIT_REPORT.md — full project audit
- docs/FRAGMENT_DEFCONFIG_DIFF.md — fragment vs tegra_defconfig comparison
- docs/POSTMARKETOS_DEFCONFIG_DIFF.md — postmarketOS config comparison
- AGENTS.md, opencode.json, .opencode/ — opencode agent config
  What was tested
- make config && make config_patch → .config diff before/after fragment cleanup is empty (exit code 0) — config is bit-identical.
- No make kernel was run.
- No boot test.
  Open issues

1. ouya.fragment spontaneously truncates — twice during this session, ouya.fragment lost lines 18-24 (USB*EHCI_TEGRA, CHIPIDEA*\*, USB_ROLE_SWITCH). Restored with git checkout both times. Root cause unknown — possibly a sed or merge_config.sh side effect. Check before every config_patch run that the file is intact.
2. Usb host instability (pre-existing) — AGENTS.md attributes it to missing DTS PHY trim parameters. The postmarketOS config comparison confirmed no Kconfig-level difference explains it: both disable CONFIG_USB_CHIPIDEA_HOST, both enable USB_TEGRA_PHY, USB_ULPI, USB_PHY. The root cause is almost certainly in tegra30-ouya.dts, not in kconfig fragments.
3. wireless.fragment is 100% redundant with tegra_defconfig (10/10 options identical). Was not removed per user instruction, but is dead weight.
   Next steps (priority order)
4. Add tegra30-ouya.dts USB PHY trim parameters — compare against decatf/linux (https://github.com/decatf/linux) DTS. Needs nvidia,xcvr-setup, nvidia,xcvr-hsslew, nvidia,hsdiscon-level, etc. on usb-phy@7d004000 and usb-phy@7d008000.
5. Fix make kernel_dtb path — Makefile:173 uses $(KERNEL_DTBS)/tegra30-ouya.dtb but kernel 6.12 installs DTB to $(KERNEL_DTBS)/nvidia/tegra30-ouya.dtb (blocking).
6. Remove wireless.fragment or confirm intent to keep it.
7. Fix Makefile:submodule-linux hardcoded tag (v6.12.91) — should read branch from .gitmodules.
8. Fix scripts/ouya_load_boot.sh — add [ -f "$ZIMAGE" ] guard.
9. Remove linux-config/check-config.old — orphaned backup.
